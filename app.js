const GameBoard = (() => {

  var squares =
    ["X", "X", "X",
      "X", "X", "X",
      "X", "X", "X"];

  return { squares };

})()

const displayController = (() => {

  const renderBoard = () => {
    
  };

  return { test1: 1, test2: 2 }
})()

const player = () => {
  return 1;
}

console.log(GameBoard.squares);